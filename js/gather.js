/**
 * Adaptation Forest — gather.js
 * Gather page interactive behaviour.
 * Sections 3–5 will add performer SVG, workshop list,
 * and getting-involved interactions here.
 */

'use strict';

document.addEventListener('DOMContentLoaded', () => {
  // Placeholder — expanded as sections are added.
});
